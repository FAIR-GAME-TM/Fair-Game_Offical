[build]
  command = "node buildComm.js"  # Runs your build script
  publish = "dist"              # Replace "dist" with your output folder

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
