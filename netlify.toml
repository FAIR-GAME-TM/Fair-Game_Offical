[build]
  base = "Fair-Game_Offical-main"   # This points to the subfolder that contains your files
  command = "node buildComm.js"     # Runs your build script
  publish = "dist"                  # Directory for deployment (generated by buildComm.js)

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
